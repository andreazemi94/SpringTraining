
spring:
  application:
    name: order-microservice
  config:
    import: configserver:http://localhost:8888
  cloud:
    config:
      uri: http://localhost:8888
      label: main
      profile: dev
      failFast: true
